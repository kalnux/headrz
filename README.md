# headrz
The 'heardz' Python script is used to check the security headers of a list of targets from a text file (targets.txt) and save the results to a CSV file.

This script assumes that the targets.txt file contains a list of IP addresses and ports in the format of IP_address:port
